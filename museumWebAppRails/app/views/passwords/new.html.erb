<div id="clearance" class="password-reset">
  <h2><%= t(".title") %></h2>
<h1> Hello </h1>
  <p><%= t(".description") %></p>

  <%= form_for :password_reset,
       # url: sign_in,

       #url: user_password_path(@current_user, token: @user.confirmation_token),
        html: { method: :put } do |form| %>
    <div class="password-field">
      <%= form.label :password %>
      <%= form.password_field :password %>
    </div>

    <div class="submit-field">
      <%= form.submit %>
    </div>
  <% end %>
</div>
